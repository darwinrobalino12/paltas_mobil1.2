<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Obligatorio desde Android 13 (API 33) para poder mostrar notificaciones;
         se sigue pidiendo en tiempo de uso (ver src/notifications/notificationService.ts),
         esto solo la declara como capacidad del app. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="true"
      android:supportsRtl="true">
      <!-- Semana 15: habilita el profiler de Android Studio también en un build
           real (no debuggable) de un dispositivo físico. No cambia el
           comportamiento de la app — solo permite conectar el profiler. -->
      <profileable android:shell="true" />
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Deep link paltasapp://... (mapa de rutas en src/navigation/linking.ts).
             Permite reconstruir, por ejemplo, DetallePuntoScreen entrando directo
             por "adb shell am start -a android.intent.action.VIEW -d paltasapp://puntos/1" -->
        <intent-filter android:autoVerify="false">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="paltasapp" />
        </intent-filter>
      </activity>
    </application>
</manifest>
